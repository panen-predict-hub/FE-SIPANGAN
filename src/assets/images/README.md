This directory is intended for image assets (PNG, JPEG, WebP, SVG illustrations) used in the application.

Images placed here can be imported directly into React components, allowing Vite to optimize and bundle them.
